package main

import (
    "fmt"
)

// read website.go to edit the website and see what port is is running on read the README.file to start the server
func main() {
    fmt.Println("Starting server...")
    startServer()
}

